#include "main.h"

/**
 * _abs - computes the absolute value of an inteeger
 * @d: the int to check
 * Return: the abslute value of int
 */
int _abs(int d)
{
	if (d >= 0)
	{
		return (d);
	}
	return (-d);
}
